#ifndef VALIDAR_DATA_HPP
#define VALIDAR_DATA_HPP

#include <string>

bool validarDataFutura(const std::string& data);
std::string solicitarDataValida();

bool validarCPF(long long cpf);
long long solicitarCPF();


#endif
